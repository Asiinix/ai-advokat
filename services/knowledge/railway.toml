[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["/services/knowledge/**"]

[deploy]
startCommand = "python -m knowledge_service"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
